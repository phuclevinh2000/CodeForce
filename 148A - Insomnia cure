


#include <stdio.h>
#include <string.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>


int main()
{
  int k,l,m,n,size,count=0;
  scanf("%d %d %d %d %d", &k, &l, &m, &n, &size);

  for(int i=1; i<=size; i++)
  {

    if(i%k==0 || i%l==0 || i%m==0 || i%n==0)
    count++;
  }
  printf("%d", count);
}
