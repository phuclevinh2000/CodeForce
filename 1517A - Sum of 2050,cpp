#include <bits/stdc++.h>

using namespace std;


int main()
{
	int t;
	cin >> t;
	while(t--)
	{
		long long n;
		cin >> n;
		if(n%2050!=0) cout << -1 << endl;
		else
		{
			long long dem=0;
			n/=2050;
			while(n>0)
			{
				dem+=n%10;
				n/=10;
			}
			cout << dem << endl;
		}
	}
} 
 
